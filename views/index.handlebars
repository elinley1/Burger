<h1>Burgers for Consumption!</h1>

<h2>Some burgers to devour.</h2>

<ul class="burger-list">
    {{#each burgers}}
        {{#unless devoured}}
            {{> burger-block devoured=false}}
        {{/unless}}
    {{/each}}    
</ul>

<h2>Some burgers you devoured.</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burger-block devoured=true}}
        {{/if}}
    {{/each}}        
</ul>

<h2>Add a burger of your choice</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burger">Burger Name:</label>
        <input type="text" id="burger" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">Devour?</label><br>
        <input type="radio" name="devoured" value="1" checked>Devour it!<br>
        <input type="radio" name="devoured" value="0"> Hold off for now
    </div>

    <a href="#" id="get-burger-btn" class="btn btn-default">Add burger!</a>
</form>